<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Error</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body class="bg-light text-center">

<div class="container py-5">
    <h1 class="display-4 text-danger">Oops! Something went wrong.</h1>

    <p class="lead mt-3"><%= message %></p>

    <% if (error && error.status) { %>
        <p>Status Code: <%= error.status %></p>
    <% } %>

    <% if (error && error.stack) { %>
        <pre class="text-start bg-white p-3 border rounded mt-4"><%= error.stack %></pre>
    <% } %>

    <a href="/" class="btn btn-primary mt-4">Go Home</a>
</div>

</body>
</html>
