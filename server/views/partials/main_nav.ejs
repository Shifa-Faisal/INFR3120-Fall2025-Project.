  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">INFR3120</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <!-- this is to link the landing page/home page-->
        <% if (title == "Media") { %>
          <a class="nav-link active" aria-current="page" href="/">
            <i class="fa fa-star fa-lg"></i> Media</a>
        <% } else { %>
            <a class="nav-link" aria-current="page" href="/">
              <i class="fa fa-star fa-lg"></i> Media</a>
        <% } %>
        </li>
        <!-- this is to link the add a review page-->
        <li class="nav-item">
        <% if (title == "Add Review") { %>
          <a class="nav-link active" aria-current="page" href="/media/add"><i class="fa-solid fa-lg fa-phone"></i> Add Review</a>
        <% } else { %>
          <a class="nav-link" aria-current="page" href="/media/add"><i class="fa-solid fa-lg fa-phone"></i> Add Review</a>
          <% } %>
        </li>
        <!-- this is to link the about page-->
        <li class="nav-item">
        <% if (title == "About") { %>
          <a class="nav-link active" aria-current="page" href="/about"><i class="fa-solid fa-lg fa-circle-info"></i> About</a>
        <% } else { %>
          <a class="nav-link" aria-current="page" href="/about"><i class="fa-solid fa-lg fa-circle-info"></i> About</a>
        <% } %>
        </li>
        <!-- this is to link the contact page-->
        <li class="nav-item">
          <% if (title == "Contact Us") { %>
          <a class="nav-link active" aria-current="page" href="/contactus"><i class="fa-solid fa-lg fa-phone"></i> Contact Us</a>
        <% } else { %>
          <a class="nav-link" aria-current="page" href="/contactus"><i class="fa-solid fa-lg fa-phone"></i> Contact Us</a>
          <% } %>
        </li>
    </div>
  </div>
</nav>